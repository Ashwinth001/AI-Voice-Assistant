# Windows AI Voice Assistant - Local only, no cloud
# Python 3.10+

# UI - system overlay
PyQt6>=6.6.0

# Audio
sounddevice>=0.4.6
numpy>=1.24.0
scipy>=1.11.0
webrtcvad>=2.0.10

# Offline speech (local models)
vosk>=0.3.45
# Optional but recommended: better accuracy (pip install faster-whisper)
faster-whisper>=1.0.0
pyttsx3>=2.90

# Human-like thinking (local LLM)
ollama>=0.3.0

# Human-like speech (natural TTS)
edge-tts>=6.1.0
pygame>=2.5.0

# Security & system
psutil>=5.9.0
WMI>=1.5.1; sys_platform == 'win32'

# Storage & learning
# (sqlite3 is stdlib)

# Optional: local embeddings for semantic learning (runs on CPU, no API)
# sentence-transformers  # uncomment if you want semantic search
