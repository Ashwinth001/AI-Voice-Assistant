# Copy to .env.deploy and fill in (NEVER commit .env.deploy)
# Oracle login password is NOT used here — use OCI API keys instead.

ASTRA_SECRET_KEY=generate-a-long-random-string
GROQ_API_KEY=your_groq_key_from_console.groq.com
OCI_REGION=ap-hyderabad-1
